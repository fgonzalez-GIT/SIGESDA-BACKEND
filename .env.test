# Database Configuration (TEST ENVIRONMENT)
DATABASE_URL="postgresql://sigesda_user:SiGesda2024!@localhost:5432/asociacion_musical_test?schema=public"

# Server Configuration
PORT=8001
NODE_ENV=test

# JWT Configuration (for future authentication)
JWT_SECRET="test_jwt_secret_key"
JWT_EXPIRES_IN="7d"

# Email Configuration (for notifications) - Disabled in tests
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="test@test.com"
SMTP_PASS="test_password"

# Logging
LOG_LEVEL="error"  # Solo errores durante tests

# Pagination
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100

# Business Rules Configuration
CUOTA_VENCIMIENTO_DIAS=10
RECIBO_NUMERACION_INICIO=1000

# File Upload (for future features)
MAX_FILE_SIZE_MB=5
UPLOAD_PATH="uploads/test/"

# Test-specific settings
SILENT_TESTS=true
